## 当前运行环境标识（纯自定义，代码未直接读取；如需在前端使用请改为 VITE_ENV 并通过 import.meta.env 访问）
ENV = 'development'

## 前端开发服务器端口
## 用于本地启动时访问地址：http://localhost:${VITE_CLI_PORT}
VITE_CLI_PORT = 8001

## 后端服务端口
## 与 VITE_BASE_PATH 组合成代理目标，例如：http://localhost:${VITE_SERVER_PORT}
VITE_SERVER_PORT = 8002

## 接口基础前缀（被 axios baseURL 与 dev 代理使用）
## - 开发时作为代理匹配前缀（必须以 / 开头）
## - 运行时 axios 将以此作为相对路径根，如：/api/user/info
VITE_BASE_API = /api

## 后端基础地址
## 示例：http://localhost  或  http://127.0.0.1
VITE_BASE_PATH = http://localhost

## 是否开启 Vue DevTools
## 可选：open / close
VITE_POSITION = close

## DevTools 打开文件所用编辑器（传入 vueDevTools 的 launchEditor）
## 常用：code（VS Code）、webstorm、idea 等
VITE_EDITOR = code
